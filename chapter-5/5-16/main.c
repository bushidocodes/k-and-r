/**
 * K&R Exercise 5-16
 * Page 121
 * Add the -d ("directory order") option, wich makes comparisons only on letters, numbers, and blanks. Make sure it works in conjuction with -f.
 * Completed
 **/

int main(void)
{
    return 0;
}